kodeverk:
  spraak:
    url: http://localhost:${wiremock.server.port}/kodeverk

no.nav.security.jwt:
  issuer:
    azuread:
      discoveryurl: http://127.0.0.1:${mock-oauth2-server.port}/azuread/.well-known/openid-configuration
      accepted-audience: someaudience